# project test for but seat reservation

### Get Available Seats:
- Endpoint: GET `/seats/{startCity}/{endCity}`
- Example: GET `/seats/Cairo/Asyut`

### Book Seat:

- Endpoint: POST `/book-seat/{tripId}/{seatNumber}`
- Example: POST `/book-seat/1/5`
